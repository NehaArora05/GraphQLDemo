schema {
 query: Query
 mutation: Mutation
}

type Query {
 allUsers: [Users]
 user(id: String): Users
}

type Mutation {
 saveUser(userId: String!,userName: String) : Users
}
type Users {
  userId: String
  userName : String
}